import {
    // makeId,
} from "./data";
import {
    // LIST_DIRS,
    LIST_CONTENT,
    LIST_NEXT_CONTENT,
    // SAVE,
    // SAVE_ALL,
    // ADD_BUCKET,
    // SAVE_BUCKET,
    // DELETE_ALL
} from "./actions";
import reduce from "lodash/fp/reduce";



const defaultState = () => ({
});


export function reducer ( state = defaultState(), action ) {
    return state;
}
